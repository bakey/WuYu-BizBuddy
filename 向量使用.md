gufei_vec 向量库使用手册

一、连接

Host:     127.0.0.1 (wuyullm 本机)
Port:     5440
Database: gufei_vec
User:     postgres
Password: 见 /root/pg_gufei_creds.txt

完整 DSN：postgresql://postgres:cc4872ac7d93535a275b4c43822faca3@127.0.0.1:5440/gufei_vec

二、表结构

chunks (
  id          text PRIMARY KEY,    -- md5(source|chunk_idx|text[:64])
  source      text,                 -- 原始文件绝对路径
  subdir      text,                 -- chemrxiv_md/MDS_md/arxiv_markdown/pt_md/html_md
  chunk_idx   int,                  -- 文件内 chunk 序号
  n_chunks    int,                  -- 该文件总 chunk 数
  txt         text,                 -- chunk 正文 (~800 字符)
  embedding   vector(1024)          -- bge-m3 dense embedding
)

索引：
- chunks_pkey (id) — 主键
- chunks_subdir_idx (subdir) — subdir 过滤
- chunks_emb_ivfflat_h — IVFFlat on embedding::halfvec(1024), lists=2048

数据量：68,804,002 chunks 跨 5 个 subdir。

三、查询 SQL 模板

SET ivfflat.probes = 5;  -- 5 是甜点位 (~20s, 召回 ~85%);
                          -- 10 → 47s, 90%+;
                          -- 20 → 77s, 95%+

SELECT id, subdir, source, chunk_idx, n_chunks, txt,
       1 - ((embedding::halfvec(1024)) <=> $1::halfvec(1024)) AS cosine_sim
FROM chunks
WHERE subdir = $2  -- 可选过滤（强烈推荐，subdir 限定能大幅加速）
ORDER BY (embedding::halfvec(1024)) <=> $1::halfvec(1024)
LIMIT 5;

⚠️ 必须用 (embedding::halfvec(1024)) <=> $1::halfvec(1024)，左右都 cast，否则不走索引。
FROM chunks
WHERE subdir = $2  -- 可选过滤（强烈推荐，subdir 限定能大幅加速）
ORDER BY (embedding::halfvec(1024)) <=> $1::halfvec(1024)
LIMIT 5;

⚠️ 必须用 (embedding::halfvec(1024)) <=> $1::halfvec(1024)，左右都 cast，否则不走索引。

四、Python 用法

import psycopg2
from sentence_transformers import SentenceTransformer

# 加载模型（一次性，可长期持有）
model = SentenceTransformer("/data/models/bge-m3_20250912_235519m", device="cpu")

# 连接
conn = psycopg2.connect(
    "postgresql://postgres:cc4872ac7d93535a275b4c43822faca3@127.0.0.1:5440/gufei_vec"
)

def search(query: str, k=5, subdir=None, probes=5):
    emb = model.encode([query], normalize_embeddings=True)[0]
    vec = "[" + ",".join(f"{x:.6f}" for x in emb) + "]"

    cur = conn.cursor()
    cur.execute(f"SET ivfflat.probes = {probes}")

    sql = """
      SELECT id, subdir, source, chunk_idx, txt,
             1 - ((embedding::halfvec(1024)) <=> %s::halfvec(1024)) AS sim
      FROM chunks
      {where}
      ORDER BY (embedding::halfvec(1024)) <=> %s::halfvec(1024)
      LIMIT %s
    """
    where = "WHERE subdir = %s" if subdir else ""
    params = [vec] + ([subdir] if subdir else []) + [vec, k]
    cur.execute(sql.format(where=where), params)
    return cur.fetchall()

现成可用的脚本：/data/gufei_vec/pg_query.py

source /root/miniconda3/etc/profile.d/conda.sh && conda activate gufei_vec
cd /data/gufei_vec
python pg_query.py --query "你的问题" --k 5 --probes 5
python pg_query.py --query "..." --subdir arxiv_markdown --k 3

五、各 subdir 内容速查

┌────────────────┬──────────────┬─────────────────────────────────────────┐
│     subdir     │     数量     │                  内容                   │
├────────────────┼──────────────┼─────────────────────────────────────────┤
│ chemrxiv_md    │ 1.3M chunks  │ ChemRxiv 化学预印本                     │
│ chemrxiv_md    │ 1.3M chunks  │ ChemRxiv 化学预印本                     │
├────────────────┼──────────────┼─────────────────────────────────────────┤
│ MDS_md         │ 3.6M chunks  │ 学术论文（MDS 项目）                    │
├────────────────┼──────────────┼─────────────────────────────────────────┤
│ arxiv_markdown │ 11.4M chunks │ arXiv 学术论文                          │
├────────────────┼──────────────┼─────────────────────────────────────────┤
│ pt_md          │ 26.2M chunks │ 中英文书籍/文献                         │
├────────────────┼──────────────┼─────────────────────────────────────────┤
│ html_md        │ 26.3M chunks │ 网页爬取内容（中英文环保/废物处理领域） │
└────────────────┴──────────────┴─────────────────────────────────────────┘

六、运维

容器： pg-gufei-vec（restart=unless-stopped，自动跟随系统重启）

# 查状态
docker ps -f name=pg-gufei-vec
docker logs --tail 50 pg-gufei-vec

# 重启
docker restart pg-gufei-vec

磁盘占用：
- chunks 表：422 GB
- IVFFlat 索引：175 GB
- 总计 ~602 GB，存在 /data/pg_gufei/

资源限制： 容器 6GB RAM，对主机其他服务无影响。

七、Tips

1. 用 subdir 过滤是免费午餐 — 把搜索范围缩小到 1-3M 行，查询从 ~20s 降到 1-5s
2. probes 调节 — 查询时设：低 probes 快但召回低，高 probes 反之
3. bge-m3 输入须 normalize：model.encode(..., normalize_embeddings=True)
4. cosine 相似度 = 1 - cosine_distance，pgvector 的 <=> 是距离

八、可选清理（不急）

OSS 上的转运 parquet（168 GB）现在没用了，要清理可以：
ossutil rm oss://gufeizhifallm/_vec_transfer/ -r -f

---
就这些。 全流程从 0 走到能查，跨了 7 天（数据搬运 + 嵌入 + 加载 + 索引）。69M 向量、421GB 数据表、175GB 索引，bge-m3 高质量语义检索可用。 有问题随时叫我。